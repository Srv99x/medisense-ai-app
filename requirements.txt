# =============================================================================
#  MediSense AI - Python Dependencies
#  File: requirements.txt
#
#  Install locally:  py -m pip install -r requirements.txt
#  Render uses this file automatically — no extra config needed.
#
#  ⭐ Versions are pinned so cloud builds are always reproducible.
# =============================================================================

# FastAPI: The web framework for our API endpoints.
fastapi>=0.111.0,<1.0.0

# Uvicorn: The ASGI server that runs FastAPI.
# [standard] includes extras like websockets & http-tools (required by Render).
uvicorn[standard]>=0.29.0,<1.0.0

# Google Generative AI: Official client for the Gemini API.
google-generativeai>=0.5.0,<1.0.0

# Python-dotenv: Loads variables from .env into os.environ.
python-dotenv>=1.0.0,<2.0.0

